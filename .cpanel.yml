---
deployment:
  tasks:
    - export DEPLOYPATH=/home/rt6p7315/public_html
    - rm -Rfd $DEPLOYPATH/build
    - cp -R $PWD/public/build $DEPLOYPATH/
    - ln -d $PWD/storage/app/public $PWD/public/storage
    - ln -d $PWD/storage/app/public $DEPLOYPATH/storage
    # - ln -s $PWD/public $DEPLOYPATH
